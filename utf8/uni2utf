#!/usr/bin/perl
use strict;
use warnings;
use 5.010;
use Encode;

foreach (@ARGV) {
    my $s;
    $s = unpack('H*', encode('utf8', chr(hex($_))));
    $s =~ s/(..)/\\x$1/g;
    say "echo -e '$s'";
}
